pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        jcenter()
        maven { url uri("https://jitpack.io") }
        maven { url uri("https://maven.aliyun.com/repository/public") }
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven { url uri("https://jitpack.io") }
        maven { url uri("https://maven.aliyun.com/repository/public") }
    }
}

rootProject.name = "themorph"
include(":app")
